#pragma once

// Displays the main start menu in a loop until the player chooses to play or exit.
// Returns true if the player wants to start a game, false if they chose to exit.
bool showStartMenu();

// Displays the control reference screen and waits for any key before continuing.
void showControlsScreen();
